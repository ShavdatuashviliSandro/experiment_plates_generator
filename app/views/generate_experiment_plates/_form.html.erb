<br>
<%= simple_form_for :experiment_plate do |f| %>
  <%= f.input :plate_size, label: 'Plate Size'%>
  <%= f.input :sample, label: 'Sample' %>
  <%= f.input :reagent, label: 'Reagent'%>
  <%= f.input :number_of_replication, label: 'Replicate' %>
  <br>
  <br>
  <%= link_to 'უკან დაბრუნება',root_url, class: 'btn btn-warning' %>
  <%= f.button :submit, class: 'btn btn-primary', value: 'Create Experiment' %>
<%  end %>